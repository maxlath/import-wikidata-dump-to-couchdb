{
  "name": "couch-wikidata-dump-importer",
  "version": "1.0.0",
  "description": "",
  "main": "build/couch-wikidata-dump-importer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build; coffee -o build -c src",
    "import": "node ./build/couch-wikidata-dump-importer.js"
  },
  "author": "maxlath",
  "license": "MIT",
  "dependencies": {
    "bluereq": "^0.2.1",
    "config": "^1.15.0"
  },
  "devDependencies": {
    "coffee-script": "^1.9.3"
  }
}

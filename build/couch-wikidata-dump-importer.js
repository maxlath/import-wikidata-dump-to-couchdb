// Generated by CoffeeScript 1.9.3
(function() {
  var CONFIG, breq, dbUrl, file, reader;

  reader = require('./reader');

  file = process.argv.slice(2)[0];

  breq = require('bluereq');

  CONFIG = require('config');

  dbUrl = CONFIG.db.dbUrl();

  reader(file, function(line) {
    var id, json;
    line = line.slice(0, -1);
    json = JSON.parse(line);
    id = json.id;
    return breq.put(dbUrl + "/" + id, json).then(function(res) {
      return console.log(res.body);
    });
  });

}).call(this);
